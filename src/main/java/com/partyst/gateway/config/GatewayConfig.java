package com.partyst.gateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * Gateway routes are now configured in application.yml
 * This file is kept for future custom route logic if needed
 */
@Configuration
public class GatewayConfig {
    // Routes configured in application.yml to avoid CORS duplication
}